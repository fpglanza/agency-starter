---
import { navigation } from "../../data/navigation";
import { site } from "../../data/site";
import { whatsappLink } from "../../lib/whatsapp";
---

<footer class="bg-[var(--color-secondary)] text-white">
  <div class="container py-12">

    <!-- TOP -->
    <div class="grid gap-10 md:grid-cols-3 items-start">

    <!-- LOGO + INFO -->
    <div class="space-y-5">
      <img
        src={site.brand.logo}
        alt={site.brand.logoAlt}
        class="h-12 w-auto object-contain"
      />

      <p class="text-sm text-white/70">
        {site.name}
        <br />
        {site.footer.locationSummary}
      </p>

      <div class="space-y-2 border-t border-white/10 pt-5 text-xs leading-6 text-white/55">
        <p class="font-semibold text-white/70">
          {site.legal.displayName}
        </p>

        <p>
          P.IVA/C.F. {site.legal.taxId}
          <br />
          Cod. SDI {site.legal.sdi}
        </p>

        <p>
          <span class="font-semibold text-white/65">Sede legale</span>
          <br />
          {site.legal.registeredOffice}
        </p>

        <p>
          <span class="font-semibold text-white/65">Legale rappresentante</span>
          <br />
          {site.legal.representative}
        </p>
      </div>
    </div>

      

      <!-- NAV -->
      <div class="flex flex-col gap-3">
        <p class="text-xs uppercase tracking-[0.12em] text-white/50">
          {site.footer.navigationLabel}
        </p>

        {navigation.map((item) => (
          <a
            href={item.href}
            class="text-sm font-semibold transition hover:text-[var(--color-primary)]"
          >
            {item.label}
          </a>
        ))}
      </div>

      <!-- CONTATTI -->
      <div class="flex flex-col gap-6">
        <p class="text-xs uppercase tracking-[0.12em] text-white/50">
          {site.footer.contactsLabel}
        </p>

        {site.locations.map((location) => (
          <div class="space-y-1">
            <p class="text-sm font-semibold text-white">
              {location.name}
            </p>

            <p class="text-sm text-white/70">
              {location.addressLines[0]}<br />
              {location.addressLines[1]}
            </p>

            <a
              href={location.phone.href}
              class="text-sm text-white/80 transition hover:text-[var(--color-primary)]"
              data-track="phone_click"
              data-location={location.trackingLocation}
              data-section="footer"
            >
              {location.phone.label}
            </a>

            <p class="text-xs leading-5 text-white/55">
              {location.hours}
            </p>

            <a
              href={whatsappLink(location.whatsapp.phone, location.whatsapp.message)}
              class="text-sm text-white/80 transition hover:text-[var(--color-primary)]"
              data-track="whatsapp_click"
              data-location={location.trackingLocation}
              data-section="footer"
            >
              {location.whatsapp.label}
            </a>
          </div>
        ))}

        <!-- LINK CONTATTI -->
        <a
          href={site.footer.contactPageLink.href}
          class="pt-2 text-sm font-semibold transition hover:text-[var(--color-primary)]"
          data-track="cta_click"
          data-location="footer"
          data-section="contacts"
        >
          {site.footer.contactPageLink.label}
        </a>
      </div>
    </div>
    <!-- DIVIDER -->
    <div class="my-10 h-px bg-white/10"></div>

    <!-- BOTTOM -->
    <div class="flex flex-col gap-4 md:flex-row md:items-center md:justify-between">
      <p class="text-xs text-white/60">
        © {site.footer.copyrightYear} {site.name}. {site.footer.copyrightNotice}
      </p>

      <div class="flex flex-wrap gap-6 text-xs">
        <a
          href={site.footer.privacyLink.href}
          class="hover:text-[var(--color-primary)]"
        >
          {site.footer.privacyLink.label}
        </a>

        <a
          href={site.footer.cookieLink.href}
          class="hover:text-[var(--color-primary)]"
        >
          {site.footer.cookieLink.label}
        </a>

        <a
          href="#"
          class="lb-cs-settings-link hover:text-[var(--color-primary)]"
        >
          {site.footer.cookieSettingsLabel}
        </a>
      </div>
    </div>

  </div>
</footer>
